import { LengthLimitForContentPipe } from './length-limit-for-content.pipe';

describe('LengthLimitForContentPipe', () => {
  it('create an instance', () => {
    const pipe = new LengthLimitForContentPipe();
    expect(pipe).toBeTruthy();
  });
});
